const SingleXaxis = (props: any) => {
  const { sessionChartData } = props;

  console.log("view config", sessionChartData);

  return <div style={{ height: "100vh" }}>single x axis</div>;
};

export default SingleXaxis;
